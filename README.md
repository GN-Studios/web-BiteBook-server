# web-bitebook-server

Basic NestJS scaffold.

Quick start

1. Install deps:

```bash
npm install
```

2. Run in development:

```bash
npm run start:dev
```

3. Build and run:

```bash
npm run build
npm start
```

The server listens on port `3000` by default.
